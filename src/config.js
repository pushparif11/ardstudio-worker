export const OPENAI_API_URL = "https://api.openai.com/v1/images/edits";

export const APP_NAME = "ARD Studio AI";

export const API_VERSION = "2.0.0";

export const CORS_HEADERS = {
  "Access-Control-Allow-Origin": "*",
  "Access-Control-Allow-Headers": "Content-Type, Authorization",
  "Access-Control-Allow-Methods": "GET, POST, OPTIONS",
  "Content-Type": "application/json"
};

export const DEFAULT_PROMPT =
  "Expand the background naturally with realistic continuation while preserving the original subject, lighting, colors, perspective and textures. No new people or objects.";
